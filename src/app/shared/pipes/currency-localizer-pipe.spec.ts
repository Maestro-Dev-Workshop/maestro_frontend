import { CurrencyLocalizerPipe } from './currency-localizer-pipe';

describe('CurrencyLocalizerPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyLocalizerPipe();
    expect(pipe).toBeTruthy();
  });
});
