import { PreferenceValidator } from './preference-validator';

describe('PreferenceValidator', () => {
  it('should create an instance', () => {
    const directive = new PreferenceValidator();
    expect(directive).toBeTruthy();
  });
});
